/** @type {import('next').NextConfig} */
const nextConfig = {
  // watchOptions removed to address "Unrecognized key(s)" warning with Next.js 14.2.4
  // If specific watch ignoring is needed, it should be verified against 14.2.4 documentation
  // for the correct configuration structure (e.g., under webpack customization).
};

export default nextConfig;
