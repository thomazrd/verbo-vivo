
match /studies/{studyId} {
  // Permite leitura pública de estudos com status 'PUBLISHED'.
  // Ninguém pode escrever diretamente, apenas o backend administrativo.
  allow read: if resource.data.status == 'PUBLISHED';
  allow write: if false; 
}
