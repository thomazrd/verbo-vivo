rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // --- Functions ---
    // Helper functions should be included first so they are available
    // to all collection rule files that follow.
    include "functions/auth.rules";

    // --- Collection Rules ---
    // {{RULES_CONTENT}}
  }
}
