'use client';

import '../../i18n'; // Import to initialize i18next

const I18nInitializer = () => {
  // This component doesn't render anything itself.
  // Its sole purpose is to ensure the i18n instance is initialized on the client-side.
  return null;
};

export default I18nInitializer;
